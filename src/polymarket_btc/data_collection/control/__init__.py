"""Control panel: a small web front-end tying the market data collector to
the (not-yet-built) live trader and backtester -- three destinations
(live trading, backtest, data collection) plus a self-serve plugin system
for adding new data feeds without touching the gateway's code."""
